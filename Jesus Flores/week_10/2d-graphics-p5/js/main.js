function setup(){
    createCanvas(windowWidth, windowHeight);
    
    // strokeWeight(10);
    // stroke(0, 0, 255);
    // noStroke();
    //      x    y   w   h
    fill(255, 0, 0);
    ellipse(50, 50, 80, 80);

    fill(255, 255, 0);
    noFill();
    stroke(255,255, 0);
    ellipse(windowWidth/2, windowHeight/2, 150, 150);
}
