<h2>Index of Pokemon</h2>

<ul class="pokemonUL">

    <% @pokemons.each do |pokemon| %>
        <li>
            <%= link_to image_tag(pokemon.image_one), pokemon_path(pokemon.id)  %>
        </li>
    <% end %>

</ul>